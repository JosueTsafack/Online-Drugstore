/**
 * Configuration
 * @module config
 */

const config = {
  name: 'Votre Pharmacie en ligne',
  description: 'Boilerplate with React and Redux with Redux Saga',
  apiUrl: 'http://localhost/swiftdrugs/api/v1' // global app config object 
};

export default config;
